import { TestBed } from '@angular/core/testing'; import { provideHttpClient } from '@angular/common/http'; import { provideHttpClientTesting, HttpTestingController } from '@angular/common/http/testing'; import { AuthService } from './auth.service';
describe('AuthService',()=>{let service:AuthService,http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting()]});service=TestBed.inject(AuthService);http=TestBed.inject(HttpTestingController);});it('stores the authenticated user after login',()=>{service.login({email:'a@b.com',password:'password'}).subscribe();http.expectOne('/api/auth/login').flush({id:'1',email:'a@b.com',displayName:'Ana',createdAtUtc:''});expect(service.isAuthenticated()).toBe(true);});});
