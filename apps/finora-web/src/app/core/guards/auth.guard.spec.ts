/* eslint-disable @typescript-eslint/no-explicit-any */
import { TestBed } from '@angular/core/testing'; import { provideRouter } from '@angular/router'; import { provideHttpClient } from '@angular/common/http'; import { provideHttpClientTesting, HttpTestingController } from '@angular/common/http/testing'; import { authGuard } from './auth.guard';
describe('authGuard',()=>{it('redirects unauthenticated users',()=>{TestBed.configureTestingModule({providers:[provideRouter([]),provideHttpClient(),provideHttpClientTesting()]});const result=TestBed.runInInjectionContext(()=>authGuard({} as any,{} as any)) as any;result.subscribe((value:any)=>expect(value.toString()).toContain('/login'));TestBed.inject(HttpTestingController).expectOne('/api/auth/me').flush({}, {status:401,statusText:'Unauthorized'});});});
