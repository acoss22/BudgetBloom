import { Component, inject } from '@angular/core'; import { Router, RouterLink, RouterLinkActive, RouterOutlet } from '@angular/router'; import { MatButtonModule } from '@angular/material/button'; import { MatIconModule } from '@angular/material/icon'; import { MatSidenavModule } from '@angular/material/sidenav'; import { MatToolbarModule } from '@angular/material/toolbar'; import { AuthService } from '../auth/auth.service';
@Component({standalone:true,imports:[RouterOutlet,RouterLink,RouterLinkActive,MatButtonModule,MatIconModule,MatSidenavModule,MatToolbarModule],template:`<mat-sidenav-container><mat-sidenav #drawer mode="side" opened><a class="brand" routerLink="/dashboard"><span>F</span> Finora</a><nav aria-label="Main navigation">@for(item of nav;track item.path){<a [routerLink]="item.path" routerLinkActive="active"><mat-icon>{{item.icon}}</mat-icon>{{item.label}}</a>}</nav></mat-sidenav><mat-sidenav-content><mat-toolbar><button mat-icon-button (click)="drawer.toggle()" aria-label="Toggle navigation"><mat-icon>menu</mat-icon></button><span class="spacer"></span><span class="user">{{auth.user()?.displayName}}</span><button mat-button (click)="logout()">Logout</button></mat-toolbar><main class="content"><router-outlet /></main></mat-sidenav-content></mat-sidenav-container>`,styles:[`mat-sidenav-container{min-height:100dvh}mat-sidenav{width:240px;padding:1.25rem;background:#143a2a;color:white}.brand{display:flex;align-items:center;gap:.75rem;color:white;text-decoration:none;font-size:1.4rem;font-weight:700;margin-bottom:2rem}.brand span{display:grid;place-items:center;width:36px;height:36px;border-radius:12px;background:#9bdd79;color:#143a2a}nav{display:grid;gap:.3rem}nav a{display:flex;gap:.8rem;align-items:center;padding:.75rem;border-radius:10px;color:#dce9e1;text-decoration:none}.active,nav a:hover{background:#285844;color:white}mat-toolbar{position:sticky;top:0;z-index:2;background:white;border-bottom:1px solid #e2e8e4}.spacer{flex:1}.content{padding:clamp(1rem,3vw,2rem);background:#f5f7f5;min-height:calc(100dvh - 64px)}@media(max-width:700px){mat-sidenav{width:210px}.user{display:none}}`]})
export class ShellComponent{auth=inject(AuthService);private router=inject(Router);nav=[['dashboard','dashboard','Dashboard'],['accounts','account_balance_wallet','Accounts'],['transactions','swap_horiz','Transactions'],['categories','category','Categories'],['budgets','savings','Budgets'],['recurring-transactions','repeat','Recurring'],['settings','settings','Settings']].map(([path,icon,label])=>({path,icon,label}));logout(){this.auth.logout().subscribe(()=>this.router.navigateByUrl('/login'));}}
